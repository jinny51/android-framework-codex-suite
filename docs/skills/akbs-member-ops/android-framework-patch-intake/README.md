# android-framework-patch-intake compatibility

Deprecated compatibility surface owned by `akbs-member-ops`. It forwards to
`akbs-patch-submit` without duplicating package or submission logic.

Use `$akbs-patch-submit` for new work.
