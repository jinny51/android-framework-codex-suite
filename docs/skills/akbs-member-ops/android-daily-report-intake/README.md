# android-daily-report-intake compatibility

Deprecated compatibility surface owned by `akbs-member-ops`. It forwards to
`akbs-daily-report`; the canonical Skill and one internal incoming v1 kernel own all
generation, validation, and submission behavior.

Use `$akbs-daily-report` for new work.
