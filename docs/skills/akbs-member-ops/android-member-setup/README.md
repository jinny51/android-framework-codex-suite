# android-member-setup compatibility

Deprecated compatibility surface owned by `akbs-member-ops`. Its script forwards the
same arguments and exit status to `akbs-member-setup`; all setup and doctor behavior is
implemented by the canonical Skill and the single internal incoming v1 kernel.

Use `$akbs-member-setup` for new work.
