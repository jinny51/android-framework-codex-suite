interface:
  display_name: "Android Windows Source Access"
  short_description: "Manage Windows SMB source mappings."
  default_prompt: "Use $android-windows-source-access to create, restore, and resolve Windows/WSL SMB mappings from local Android source paths to remote Linux build roots."

policy:
  allow_implicit_invocation: true
