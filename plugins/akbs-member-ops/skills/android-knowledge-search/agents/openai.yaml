interface:
  display_name: "Android Knowledge Search (Compatibility)"
  short_description: "Forward legacy search requests to AKBS search"
  default_prompt: "Use $android-knowledge-search only to forward this legacy request to $akbs-knowledge-search."

policy:
  allow_implicit_invocation: false
