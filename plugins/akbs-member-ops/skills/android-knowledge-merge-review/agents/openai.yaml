interface:
  display_name: "Android Merge Review (Compatibility)"
  short_description: "Forward legacy merge review to the AKBS Skill"
  default_prompt: "Use $android-knowledge-merge-review only to forward this legacy request to $akbs-knowledge-merge-review."

policy:
  allow_implicit_invocation: false
