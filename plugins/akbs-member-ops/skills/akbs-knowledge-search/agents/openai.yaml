interface:
  display_name: "AKBS Knowledge Search"
  short_description: "Search reusable team knowledge assets"
  default_prompt: "Use $akbs-knowledge-search to search the team knowledge repository for prior Android engineering cases, platform variants, patches, search anchors, and validation evidence before reimplementing a change in App or GMS, platform, native, HAL, kernel, device, or build layers."

policy:
  allow_implicit_invocation: true
