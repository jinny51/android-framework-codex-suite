interface:
  display_name: "Android Knowledge Merge Review"
  short_description: "Review AKBS merge evidence and respond explicitly"
  default_prompt: "Use $android-knowledge-merge-review to inspect an AKBS merge confirmation, compare its evidence, and submit a dispute only when I explicitly authorize it."

policy:
  allow_implicit_invocation: true
