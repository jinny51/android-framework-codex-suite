interface:
  display_name: "Android Framework Patch Intake"
  short_description: "Generate Framework patch incoming packages"
  default_prompt: "Use $android-framework-patch-intake to generate, check, submit, or complete queue information for one validated Android Framework patch package. Keep patch files immutable; recapture only when source changes are required."

policy:
  allow_implicit_invocation: true
